global:
  scrape_interval: 15s
rule_files:
  - /etc/prometheus/alerts.yml
  - /etc/prometheus/slo.yml
scrape_configs:
  - job_name: fccp-api
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: local-metrics-demo-only
    static_configs:
      - targets: ["api:8080"]
